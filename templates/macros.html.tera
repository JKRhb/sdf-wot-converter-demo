{% macro textarea(label, name, placeholder="", value="", max=250) %}
    <label for="{{ name }}">{{ label }}</label>
    <textarea placeholder="{{ placeholder }}"
        name="{{ name }}" id="{{ name }}" rows="8" cols="40"
    >{{ value }}</textarea>
{% endmacro input %}
